import SwiftUI

enum RunwaySurface {
    static let cornerRadius: CGFloat = 8
    static let fill = Color(nsColor: .systemGray).opacity(0.16)
    static let subtleFill = Color(nsColor: .systemGray).opacity(0.09)
}
